---
name: dockercraft:compose
description: Generate docker-compose.yml with service detection from dependencies
argument-hint: "[services...]"
---

Invoke the `dockercraft:generating-compose` skill and follow it exactly.
