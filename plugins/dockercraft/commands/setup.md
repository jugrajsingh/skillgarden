---
name: dockercraft:setup
description: Set up complete Docker environment (Dockerfile + compose + services)
argument-hint: "[project-path]"
---

Invoke the `dockercraft:setting-up` skill and follow it exactly.
