---
name: dockercraft:dockerfile
description: Generate optimized Dockerfile and .dockerignore with language detection
argument-hint: "[language]"
---

Invoke the `dockercraft:generating-dockerfile` skill and follow it exactly.
