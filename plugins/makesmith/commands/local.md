---
name: makesmith:local
description: Generate Makefile.local with local development targets (venv, install, test, lint, format)
argument-hint: "[project-path]"
---

Invoke the `makesmith:generating-local` skill and follow it exactly.
