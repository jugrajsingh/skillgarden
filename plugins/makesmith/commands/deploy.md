---
name: makesmith:deploy
description: Generate Makefile.deploy with DevOps targets (build, push, deploy via Helm/kubectl)
argument-hint: "[project-path]"
---

Invoke the `makesmith:generating-deploy` skill and follow it exactly.
