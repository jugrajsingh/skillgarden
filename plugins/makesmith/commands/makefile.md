---
name: makesmith:makefile
description: Generate root Makefile that delegates to Makefile.local and Makefile.deploy
argument-hint: "[project-path]"
---

Invoke the `makesmith:generating-makefile` skill and follow it exactly.
