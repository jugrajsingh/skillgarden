---
name: shipit:execute
description: Execute plan with subagent pipeline (implement → review → verify)
argument-hint: "<task_plan.md path or slug>"
---

Invoke the `shipit:executing` skill and follow it exactly.
