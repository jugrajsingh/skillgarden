---
name: pysmith:setup
description: Set up complete Python dev environment (pyproject + settings + pre-commit + makefile)
argument-hint: "[project-path]"
---

Invoke the `pysmith:setting-up` skill and follow it exactly.
